<h1>I am the Posts form</h1>

<%= form_for @post do |f|%>

<%= f.label :title%>
<%= f.text_field :title%>

<%= f.label :content%>
<%= f.text_area :content%>
<%= f.collection_select :blogger_id, Blogger.order(:name), :id, :name %>
<%= f.collection_select :destination_id, Destination.order(:name), :id, :name%>
<%= f.submit  %>
<% end  %>


